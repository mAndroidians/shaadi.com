package com.shaadi.util


object ImageBindingAdapter {
/*    @JvmStatic
    @BindingAdapter(value = ["android:src"])
    fun setImageUrl(imageView: ImageView, url: String) {
        GlideApp.with(imageView.context)
            .load(url)
            .placeholder(R.drawable.yuktan_logo)
            .fitCenter()
            .into(imageView)
    }

    @JvmStatic
    @BindingAdapter(value = ["android:src"])
    fun setImageUrl(imageView: ImageView, url: Int) {
        GlideApp.with(imageView.context)
            .load(url)
            .placeholder(R.drawable.yuktan_logo)
            .fitCenter()
            .into(imageView)
    }*/
}